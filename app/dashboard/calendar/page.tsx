import { ComingSoon } from "@/components/coming-soon";

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Calendar"
      description="Interviews, calls, and other events linked to your pipeline, pulled from Google Calendar."
    />
  );
}
